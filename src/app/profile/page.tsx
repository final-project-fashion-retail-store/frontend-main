import ProfileContainer from '@/components/profile/ProfileContainer';

const Profile = () => {
	return (
		<div className='container mx-auto py-8 border-b'>
			<ProfileContainer />
		</div>
	);
};

export default Profile;
